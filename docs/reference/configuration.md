# Configuration

- Authentication mode is OAuth2 with PKCE or a Personal Access Token.
- Integration options control polling intervals, device filters, location tracking, request budgeting, and an optional compiled OpenAPI specification path.
- OAuth scopes determine optional location, action, remote-access, and PoE features.

Secrets are referenced from protected runtime stores and must never be committed.
