# Concepts

Home Assistant custom integration for monitoring and controlling supported Teltonika RMS-managed networking devices.

The repository executes as Home Assistant custom integration runtime and uses Python, Home Assistant, Teltonika RMS API, OAuth2/PAT, Socket.IO.
