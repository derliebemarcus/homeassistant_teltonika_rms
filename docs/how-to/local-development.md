# Local development

1. Run `make setup` to create the development environment and hooks.
2. Use the Makefile targets for linting, tests, contracts, security, and lockfiles.
3. Keep Home Assistant tests and unit tests passing before opening a pull request.

Keep repository-specific secrets outside Git and preserve generated-file boundaries.
